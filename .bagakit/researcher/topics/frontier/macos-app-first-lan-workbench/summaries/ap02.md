# macOS signing and notarization summary

## What This Is

- source id: `ap02`

## Why It Matters

The user wants to compile/install the app on source and target machines, so distribution trust and executable provenance are product requirements.

## Borrow
- Treat Developer ID signing/notarization and entitlement display as part of the implementation plan.

## Avoid
- Do not leave binary provenance, bundled CLI version, or Gatekeeper behavior as implicit release work.

## Bagakit Implication
- Add packaging acceptance for signed/notarized app or explicitly documented local developer build path.
