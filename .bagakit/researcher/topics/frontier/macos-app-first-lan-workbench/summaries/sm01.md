# Current plan summary

## What This Is

- source id: `sm01`

## Why It Matters

The plan is the implementation contract for the app-first LAN workbench.

## Borrow
- Keep evidence-backed truth language, source/target/observer roles, and explicit non-goals.

## Avoid
- Do not keep the current ordering where UI wiring precedes structured event and process contracts.

## Bagakit Implication
- Revise the task graph before implementation so acceptance criteria match app-first setup, LAN command coverage, and validation gates.
