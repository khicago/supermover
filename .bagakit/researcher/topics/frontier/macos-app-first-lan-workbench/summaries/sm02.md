# Current implementation surface summary

## What This Is

- source id: `sm02`

## Why It Matters

Implementation evidence separates available CLI behavior from current macOS app coverage.

## Borrow
- The CLI already exposes profile init/lint/set-target, discover, pair, sync queue/run/loop/watch/network, daemon, serve, status/report/health JSON surfaces.

## Avoid
- Do not claim the app orchestrates LAN discovery, pairing, sync queue/network run, or daemon lifecycle until SuperMoverTaskKind and app state models cover them.

## Bagakit Implication
- Add a command coverage matrix and role-scoped process supervisor before transfer UI completion.
